version: '3.8'

services:
  portfolio-optimizer:
    build: .
    ports:
      - "8501:8501"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - .:/app
    command: streamlit run portfolio_optimizer.py --server.port=8501 --server.address=0.0.0.0
